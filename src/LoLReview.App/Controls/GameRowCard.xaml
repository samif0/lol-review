<UserControl
    x:Class="LoLReview.App.Controls.GameRowCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Border x:Name="HostBorder"
            Background="{StaticResource CardBackgroundBrush}"
            BorderBrush="{StaticResource SubtleBorderBrush}"
            BorderThickness="1"
            CornerRadius="2"
            Padding="0"
            PointerEntered="OnPointerEntered"
            PointerMoved="OnPointerMoved"
            PointerExited="OnPointerExited">
        <Grid>
            <Border x:Name="GlowOverlay"
                    CornerRadius="2"
                    IsHitTestVisible="False"
                    Opacity="0">
                <Border.Background>
                    <RadialGradientBrush x:Name="GlowBrush"
                                         Center="0.5,0.5"
                                         GradientOrigin="0.5,0.5"
                                         RadiusX="1"
                                         RadiusY="1">
                        <GradientStop Color="#10A78BFA" Offset="0"/>
                        <GradientStop Color="#05A78BFA" Offset="0.4"/>
                        <GradientStop Color="#00A78BFA" Offset="1"/>
                    </RadialGradientBrush>
                </Border.Background>
            </Border>

            <!-- Hover gradient sweep -->
            <Rectangle x:Name="SweepRect"
                       HorizontalAlignment="Stretch"
                       VerticalAlignment="Stretch"
                       Opacity="0"
                       IsHitTestVisible="False">
                <Rectangle.Fill>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#14A78BFA" Offset="0"/>
                        <GradientStop Color="#00A78BFA" Offset="1"/>
                    </LinearGradientBrush>
                </Rectangle.Fill>
            </Rectangle>

            <Rectangle x:Name="TopHighlight"
                       Height="1"
                       VerticalAlignment="Top"
                       HorizontalAlignment="Stretch"
                       Opacity="0"
                       IsHitTestVisible="False">
                <Rectangle.Fill>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#00A78BFA" Offset="0"/>
                        <GradientStop Color="#80A78BFA" Offset="0.5"/>
                        <GradientStop Color="#00A78BFA" Offset="1"/>
                    </LinearGradientBrush>
                </Rectangle.Fill>
            </Rectangle>

            <Grid ColumnSpacing="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Rectangle x:Name="WinLossBar"
                           Grid.Column="0"
                           HorizontalAlignment="Stretch"
                           VerticalAlignment="Stretch"/>

                <StackPanel Grid.Column="1" VerticalAlignment="Center" Padding="0,6,0,6" Spacing="1">
                    <TextBlock x:Name="ChampionTextBlock"
                               FontFamily="{StaticResource HeadingFont}"
                               FontSize="13"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource PrimaryTextBrush}"
                               TextTrimming="CharacterEllipsis"
                               MaxLines="1"/>
                    <TextBlock x:Name="MetaTextBlock"
                               FontFamily="{StaticResource MonoFont}"
                               FontSize="8"
                               Foreground="{StaticResource MutedTextBrush}"
                               TextTrimming="CharacterEllipsis"
                               MaxLines="1"/>
                </StackPanel>

                <TextBlock x:Name="KdaTextBlock"
                           Grid.Column="2"
                           FontFamily="{StaticResource MonoFont}"
                           FontSize="9"
                           Foreground="{StaticResource SecondaryTextBrush}"
                           VerticalAlignment="Center"
                           Margin="0,0,4,0"/>

                <ContentPresenter x:Name="ActionsHost"
                                  Grid.Column="3"
                                  VerticalAlignment="Center"
                                  Margin="0,0,6,0"/>

                <!-- Hidden but kept for binding -->
                <TextBlock x:Name="StatsTextBlock" Visibility="Collapsed"/>
            </Grid>
        </Grid>
    </Border>
</UserControl>
